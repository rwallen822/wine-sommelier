# Copy this to .env and fill in your values
# For Vercel deployment, add these as Environment Variables in the dashboard

# Your Anthropic API key (required for the Chat/bottle scanning feature)
# Get one at: https://console.anthropic.com
# NOTE: The current build calls the API directly from the browser.
# For production, you should set up a serverless proxy (see README).
VITE_ANTHROPIC_API_KEY=sk-ant-...
